{% extends 'backend/base.html.twig' %}

{% block title %}Afficher un voyageur{% endblock %}

{% block body %}
    <div class="col-10 p-0">
        <div class="p-3">
            <h1>Afficher un voyageur</h1>
            <div class="row">
                <div class="col-6">
                    <p><strong>Id :</strong> {{ traveler.id }}</p>
                    <p><strong>Nom :</strong> {{ traveler.lastname }}</p>
                    <p><strong>Prénom :</strong> {{ traveler.firstname }}</p>
                    <p><strong>Email :</strong> {{ traveler.email }}</p>
                    <p><strong>Numéro de téléphone :</strong> {{ traveler.telNumber }}</p>
                    <p><strong>Dernière connexion :</strong> {{ traveler.lastConnection|date('d/m/Y H:i:s') }}</p>
                    <p><strong>Inscrit le :</strong> {{ traveler.dateInscription|date('d/m/Y H:i:s') }}</p>
                    <p><strong>Rôles :</strong> 
                    {% for role in traveler.roles %}
                        {{ role }}
                    {% endfor %}
                    </p>
                    {% if traveler['documents'] is defined and traveler['documents'] is not empty %}
                        <div class"container"><strong>Documents :</strong>
                            <ul>
                                {% for document in traveler.documents %}
                                    <li><a href={{ document.url }} target="blank_" class="btn" style="background-color: #A594F9;">{{ document.name }}</a></li>
                                {% endfor %}
                            </ul>
                        </div>
                    {% endif %}
                <div class="col-6">
                    <p><strong>Photo :</strong></p>
                    <img src="{{ asset('profiles/') }}" alt="{{ traveler.firstname }}" class="img-fluid">
                </div>
                
        </div>
    </div>
{% endblock %}